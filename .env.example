# Supabase is OPTIONAL. The boilerplate runs without it (middleware becomes a
# no-op and the Supabase clients throw a helpful error if used). Uncomment and
# fill these in to enable Supabase auth + database.
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
